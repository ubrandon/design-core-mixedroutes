---
description: Core rules for the Design Core tool -- guides AI to help designers create screens and interactive prototypes
alwaysApply: true
---

# Design Core -- AI Guidelines

## Context Detection -- Core Repo vs Consumer Repo

Before doing anything, determine which context you are operating in:

### How to detect

Check the **repo name** (the workspace folder name or `package.json` `name` field):

- If the repo is called **`design-core`**, you are in the **core repo**.
- If the repo is called anything else, you are in a **consumer repo** (a project that uses the tool).

### Core repo mode (developing the tool itself)

You are working with a **developer** who maintains the design tool. In this mode:

- The goal is to improve, fix, or extend the tool -- its pages, scripts, styles, Vite config, Cursor rules, etc.
- You **can and should** edit root-level HTML pages (`index.html`, `project.html`, `canvas.html`, `prototype.html`, `design-system.html`), **`public/scripts/`** (browser tool JS), repo-root **`scripts/`** (Node CLIs only), config files, shared styles, and anything else in the repo.
- Do NOT create new design projects, screens, or prototypes unless the developer explicitly asks (e.g. for testing). The `public/data/projects/` folder contains sample/test data -- treat it as fixtures, not real design work.
- Speak technically. The developer understands HTML, CSS, JS, Vite, and the codebase.
- The "Critical Rules" section below about staying in a project folder and never editing the tool **does not apply** -- those rules protect designers in consumer repos.

### Consumer repo mode (using the tool to design)

You are helping a **designer** create UI screens and interactive prototypes. They may not be technical. Be helpful, visual, and avoid jargon.

- All the rules below (project boundaries, prototype mode, design system usage, tone) apply fully.
- **Before every file create or edit**, run the safe-path check described in "Pre-flight safe-path check" below. If the path fails the check, **stop immediately** and warn the user before proceeding.
- Never modify tool internals on your own initiative. Your job is to help them build screens and prototypes inside their project folder.

### NEVER make tool-level changes in a company repo

**This is critical.** Company repos receive tool updates by pulling from the upstream core repo (`ubrandon/design-core`). If you make tool-level changes directly in a company repo, those changes will conflict with or be overwritten by future upstream merges.

**Tool-level files** are anything that is NOT project data or designer identity. This includes:

- `.cursor/rules/` -- all Cursor rule files (`design-tool.mdc`, `setup.mdc`, etc.)
- `GETTING_STARTED.md`, `README.md`
- Root HTML pages (`index.html`, `project.html`, `canvas.html`, `prototype.html`, `design-system.html`)
- `public/scripts/` -- JavaScript that powers the tool in the browser (must live here so `vite build` deploys it to GitHub Pages)
- `scripts/` (repo root) -- Node-only CLIs (`capture-screens.js`, `sync-public-site-url.js`), not loaded by HTML
- `public/styles/shared.css`, `public/styles/ds.css` -- shared design system styles
- `vite.config.js`, `package.json`, `package-lock.json`
- `.github/` -- GitHub Actions workflows
- `docs/` -- documentation files

**What to do instead:**

- If a tool-level fix or improvement is needed, tell the user: "This change belongs in the core Design Core tool, not your company repo. Reach out to **Brandon Unglaub** if you need a change to the tool."
- The ONLY tool-level files a company repo should modify during setup are `GETTING_STARTED.md` (to set the company's clone URL) and `.designer.example` (to set the company name). After that initial setup commit, these files should only change via upstream pulls.

**What you CAN create and edit in a company repo (user content):**

All user-created content lives under `public/data/`. This is the boundary between the tool and the team's work.

- `public/data/projects/` -- all project folders, screens, prototypes, and project JSON files
- `public/data/design-system/components/<company-slug>/` -- company-specific design system component previews. **Always use a company subfolder** (e.g. `components/acme/`), never put files directly in `components/`. See "User content separation" below.
- `public/data/design-system/registry.json` -- registry for your design system components (ships empty, you fill it)
- `public/data/design-system/company.css` -- company brand overrides (colors, fonts, radii). Loaded automatically after `shared.css` and `ds.css`. Use this instead of editing `shared.css` or `ds.css`.
- `.designer` -- the local identity file (gitignored, never committed)

### Pre-flight safe-path check

**In consumer repo mode, you MUST run this check before every file create, edit, or delete.** This is the single most important guardrail in the tool. Skipping it risks breaking the tool for the entire team.

**Step 1 -- Check the file path against the SAFE list.** The file is safe to edit if its path matches ANY of these patterns:

| Safe pattern                                             | What it covers                                |
| -------------------------------------------------------- | --------------------------------------------- |
| `public/data/projects/**`                                | All project screens, prototypes, and metadata |
| `public/data/design-system/components/<company-slug>/**` | Company-specific component previews           |
| `public/data/design-system/registry.json`                | Design system registry                        |
| `public/data/design-system/company.css`                  | Company brand overrides                       |
| `public/data/captures/config.json`                       | Screen capture settings                       |
| `public/data/site.json`                                  | Public URL config                             |
| `.designer`                                              | Local identity file                           |
| `.app-screens.json`                                      | Local capture credentials                     |

If the path matches a safe pattern, proceed normally.

**Step 2 -- If the path does NOT match any safe pattern, it is a CORE TOOL FILE.** Stop and show the user a clear warning:

> "That file (`<path>`) is part of the Design Core tool itself, not your project data. I can't edit core tool files in your company repo -- those changes will conflict with future tool updates.
>
> If you need a change to the tool itself, reach out to **Brandon Unglaub** -- he maintains the core Design Core repo and can make the update so it flows down to all teams."

**Do NOT proceed with the edit.** The only exception is if the user explicitly confirms they understand the risks and insists. If they do, remind them that the change will likely be overwritten or cause merge conflicts on the next upstream pull.

**Step 3 -- Common traps to watch for.** Even well-intentioned requests can lead to core file edits. Watch for these:

- "Fix the nav bar" -- if they mean the tool's navigation (in `index.html`, `project.html`, etc.), that's a core file. If they mean a nav bar inside their prototype, that's safe.
- "Change the font" / "Update the colors" -- if they want to change `shared.css` or `ds.css`, redirect them to `company.css`. If they want to change fonts inside a prototype, that's safe.
- "The page isn't loading right" -- if the fix requires editing `public/scripts/`, `vite.config.js`, or a root HTML page, that's a core file. Tell them to reach out to **Brandon Unglaub** to report the bug.
- "Add a new page to the tool" -- adding a new root HTML page is a core change. If they want a new screen or prototype, that goes in their project folder.
- "Update the build" / "Fix deployment" -- `vite.config.js`, `.github/`, `package.json` are all core files. Direct them to **Brandon Unglaub** for tool changes.

### Git rules

- **NEVER commit or push without the user explicitly asking.** Do not auto-commit, do not commit as part of a multi-step task, do not commit "to save progress." The user controls when commits happen.
- **NEVER run `git fetch`, `git pull`, `git merge`, or any command that syncs with a remote** without the user explicitly asking. These can introduce unexpected changes.
- If the user asks to pull updates from the core repo, do it in two steps:
  1. Run `git fetch upstream` to download the changes
  2. Run `git log HEAD..upstream/main --oneline` to show the user what's incoming
  3. **Wait for the user to confirm** before running `git merge upstream/main` -- the merge will auto-commit, so the user should see what's coming first.

---

## First-Time Setup

When a user opens the repo for the first time, they may not have a `.designer` file yet. See `setup.mdc` for the full onboarding flow -- it walks them through identity, team info, and getting started. If `.designer` is missing and the user asks to create a project or screen, prompt them to complete setup first.

## What This Project Is

This is a design prototyping tool. Designers create:

- **Canvas screens** -- Static HTML/CSS files for UI ideation. No JavaScript.
- **Prototypes** -- Interactive HTML/CSS/JS mini apps that feel like real features. JavaScript is expected.

There is no backend, no database, no framework. The tool itself is vanilla HTML served by Vite.

## How paths work

Vite serves the `public/` folder as the web root. All data files live under `public/` on disk, but are accessed without the `public/` prefix in the browser and in fetch URLs.

- **Disk path**: `public/data/projects/my-app/project.json`
- **Browser/fetch path**: `data/projects/my-app/project.json`

When creating or editing files, always use the **full disk path** (with `public/`). When referencing URLs in HTML `src`, `href`, or `fetch()` calls, omit `public/`.

## Project Structure

All paths below are **disk paths** (relative to repo root):

- `public/data/projects/<project>/` -- One folder per project. The **project id** is the folder name (e.g. `my-app`).
- `public/data/projects/<project>/project.json` -- **Project name and metadata.** Contains `name`, `description`, and optionally `createdBy`, `createdAt`, `updatedAt` (shown on the project hub with relative dates when present). Set `updatedAt` when the project meaningfully changes — the **home** page uses it for "Recently updated" sorting.
- `public/data/projects/index.json` -- List of all projects (`id`, `name`, `description`, `createdBy`, `createdAt`). The home page also loads each project's `project.json`, `canvas.json`, and `prototypes/index.json` for search, sort, and screen/prototype counts on cards.
- `public/data/site.json` -- Optional tool config: `{ "publicBaseUrl": "https://org.github.io/repo/" }` so **Copy link** for prototypes uses the GitHub Pages URL while developing on localhost (people you share with open that link in a browser, not Cursor). Run **`npm run sync-public-url`** to fill it from `git remote get-url origin`. On `*.github.io`, the base is inferred from the current URL if this is omitted.
- `public/data/projects/<project>/canvas.json` -- **Canvas screen layout.** Lists which screens appear on the canvas and their positions/sizes. Contains `{ "screens": [{ "file": "home.html", "x": 0, "y": 0, "width": 390 }, ...] }`. When adding a new screen, you MUST also add an entry here or it won't show on the canvas. Standard widths: **390** for mobile, **1024** for tablet, **1440** for desktop. Use whatever fits the screen being designed.
- `public/data/projects/<project>/screens/` -- Static screen HTML files for the ideation canvas
- `public/data/projects/<project>/prototypes/<prototype>/index.html` -- Interactive prototype
- `public/data/projects/<project>/prototypes/<prototype>/meta.json` -- Prototype name and description; optional `thumbnail` (filename or path under the prototype folder, e.g. `preview.png`) for the project page card image.
- `public/data/projects/<project>/prototypes/index.json` -- Prototype list (`id`, `name`, `description`, optional `device`). Device can be `"mobile"` (phone frame, 390x844), `"online"` (small desktop frame, 1280x800), `"desktop"` (1024x768), or `"responsive"` (fluid).
- `public/data/design-system/components/` -- Design system component HTML previews. In company repos, use `components/<company-slug>/` subfolder.
- `public/data/design-system/registry.json` -- Category list + groups (order controls layout). Ships empty; company fills it.
- `public/data/design-system/company.css` -- (Company repos only) Brand overrides for colors, fonts, radii. Auto-loaded after `shared.css` and `ds.css`.
- `public/styles/shared.css` -- Design tokens (colors, fonts, spacing, radii). **Tool file -- do not edit in company repos.**
- `public/styles/ds.css` -- Reusable component styles. **Tool file -- do not edit in company repos.**
- **Designer identity (`.designer` file)** -- In the repo root (gitignored). Created during first-time setup (see `setup.mdc`). Contains `name`, `company`, and `team` array:
  ```json
  {
    "name": "Ben",
    "company": "Acme Inc",
    "team": [{ "name": "Ben" }, { "name": "Sara" }]
  }
  ```
  Use the `name` field for `createdBy` when creating projects. If `.designer` is missing, prompt the user to run the setup flow.

### Design System Page Layout

The design system page (`design-system.html`) uses an **infinite canvas**: main sections (groups) stack **vertically**; under each, sub-categories are laid out **horizontally** in one row so the canvas uses width. Layout is driven by `registry.json`:

- **`groups`** -- order defines vertical order. Each group has a header, then a single horizontal row of section cards (its categories).
- **Categories** -- each has `group`, `name`, and `components`. Optionally `"layout": "row"` so that section's components sit side by side.
- **Adding content** -- add a category to a group; it appears as another card in that group's horizontal row.

## Critical Rules

> These rules apply in **consumer repo mode**. In **core repo mode**, you are a developer working on the tool itself -- these boundaries do not apply (see "Context Detection" above).

1. **Run the pre-flight safe-path check on every file operation.** Before creating, editing, or deleting any file, check it against the safe-path list in "Pre-flight safe-path check" above. If the path is not on the safe list, **stop and warn the user** before proceeding. This is non-negotiable.
2. **Stay in your project.** Only create or edit files inside the designer's current project folder (`public/data/projects/<project>/`). NEVER modify files in other project folders.
3. **Stay in your prototype.** If the designer is working on a prototype (`public/data/projects/<project>/prototypes/<prototype>/`), all work happens inside that prototype's folder. Never touch other prototypes or project-level screens.
4. **Never edit the tool itself.** Do not touch root-level HTML pages (`index.html`, `project.html`, `canvas.html`, `prototype.html`, `design-system.html`), `public/scripts/`, repo-root `scripts/` CLIs, or config files. The pre-flight check enforces this, but if you're ever unsure, **ask first**.
5. **Never edit the design system** without being explicitly asked. The files in `public/data/design-system/` and `public/styles/` are shared across all projects. `company.css` and `registry.json` under `public/data/design-system/` are safe; `shared.css` and `ds.css` under `public/styles/` are NOT.
6. **Use the design system.** Always use existing CSS classes from `public/styles/ds.css` and design tokens from `public/styles/shared.css`. Read both files before creating anything.

## Before Creating Anything

When a designer asks you to create screens, prototypes, or any design work, **always check existing projects first** before creating anything new.

1. **Read `public/data/projects/index.json`** to see what projects already exist.
2. **If the user explicitly says "create a new project"**, proceed with the project creation flow below.
3. **If the user does NOT mention creating a new project** (e.g. they say "make me a home screen" or "design a login flow"), list the existing projects and ask where they want the work to go:
   - "You have these projects: **Project A**, **Project B**. Which one should I add this to, or would you like to create a new project?"
4. **If there are no projects yet**, tell the user you'll create one and ask for a name:
   - "There aren't any projects yet. What would you like to call this project?"
5. **Never auto-generate a project name** like "New Project" or "Untitled". Always ask the user.

## How to Create Things

### Creating a new project

1. Choose a project id (lowercase, hyphenated, e.g. `mobile-redesign`)
2. Create the folder: `public/data/projects/<id>/`
3. Create `public/data/projects/<id>/project.json`:
   ```json
   {
     "name": "Mobile Redesign",
     "description": "...",
     "createdBy": "Ben",
     "createdAt": "2026-03-15T00:00:00Z",
     "updatedAt": "2026-03-15T00:00:00Z"
   }
   ```
   (`updatedAt` optional; omit on first creation or mirror `createdAt`. Refresh it when the project changes so the home page sort stays accurate.)
4. Add an entry to `public/data/projects/index.json` with matching `id`, `name`, `description`, `createdBy`, `createdAt`
5. Create `public/data/projects/<id>/canvas.json`: `{ "screens": [] }`
6. Create `public/data/projects/<id>/screens/` directory
7. Create `public/data/projects/<id>/prototypes/index.json`: `{ "prototypes": [] }`

### Creating a new canvas screen

1. Create the HTML file: `public/data/projects/<project>/screens/<name>.html`
2. **Add an entry to `canvas.json`** -- this is required or the screen won't appear on the canvas:
   ```json
   { "file": "<name>.html", "x": 0, "y": 0, "width": 390 }
   ```
   Space screens apart by adjusting `x` (add the width + 40px gap for each new screen). Standard widths: **390** for mobile, **1024** for tablet, **1440** for desktop. Match the width to what the screen is designed for -- a desktop dashboard should be 1440, a mobile screen should be 390.
3. Use the standard screen wrapper:
   ```html
   <div style="background: var(--bg-0);">
     <!-- Screen content -->
   </div>
   ```
   The wrapper is just a background container. Do NOT add `padding`, `max-width`, or `margin: 0 auto` to it -- those prevent full-width elements (nav bars, hero backgrounds, footers) from reaching the edges. Individual content sections inside the wrapper handle their own padding (e.g. `padding: 16px`).
4. **HTML + CSS only.** No JavaScript. No interactivity.

#### Canvas screen content rules

Screens render inside an iframe at exactly the width set in `canvas.json`. Content that overflows will be clipped by the device frame. Follow these rules to avoid layout breakage:

- **Do NOT use `min-height: 100vh`.** The iframe auto-sizes to fit the content height. Using `100vh` creates unnecessarily tall cards on the canvas.
- **Do NOT use full HTML document structure** (`<!DOCTYPE>`, `<html>`, `<head>`, `<body>`). Canvas screens are loaded into iframes that handle this. Write the screen as a simple HTML fragment with a root wrapper `<div>`. If you need design system styles, add `<link>` tags before the wrapper -- but do NOT wrap in `<html>`/`<body>`.
- **Use flexible layouts for label/value rows.** Never use fixed-width grid columns (e.g. `grid-template-columns: 1fr 1fr`) for label/value pairs -- long values like addresses or account names will clip. Instead use flexbox with `justify-content: space-between` and let the value side wrap naturally:
  ```html
  <div
    style="display:flex; justify-content:space-between; align-items:flex-start; gap:12px; padding:10px 0;"
  >
    <span style="...label styles...">Label</span>
    <span style="...value styles...; text-align:right;">Value text here</span>
  </div>
  ```
- **Use design system classes** over inline styles whenever a matching class exists (e.g. use `jh-row`, `jh-card`, `jh-label` if the company has defined them, or `ds-list-item` from the base system). Only use inline styles for layout composition or one-off spacing.
- **Do NOT hardcode `max-width` on value text** (e.g. `max-width: 55%`). Let the flex layout handle wrapping. If a value is long, the right-aligned text will wrap naturally within the available space.
- **When creating multiple screen variants** for ideation, vary the composition and visual hierarchy (card grouping, section structure, emphasis on different elements) -- do NOT reinvent the underlying row/label mechanics each time. Reuse the same label/value pattern across variants.
- **Do not show horizontal scrollbars** on canvas screens (or prototypes). Horizontal strips (chips, carousels, story rails) should still scroll sideways via touch/trackpad/shift+wheel, but the scrollbar gutter should be hidden. Add the utility class **`ds-scroll-x`** from `shared.css` to those scroll containers (it sets `overflow-x: auto` and hides scrollbars in Firefox, WebKit, and IE). Prefer **`overflow-x: clip`** (or `hidden`) on the screen root if needed so the document itself never gains a horizontal scrollbar. **Vertical** page scrolling is normal on desktop — do not hide the main vertical scrollbar unless the design is an intentional full-screen app shell with its own scroller.

### Creating a new prototype

1. Ask the designer: "Should this be mobile or online (desktop)?" Only ask if they haven't already specified. If they say "online" or "desktop" or "web", use `"device": "online"`. If they say "mobile" or "phone" or "app", use `"device": "mobile"`. The `responsive` option also exists but rarely needed.
2. Choose a prototype id (lowercase, hyphenated, e.g. `signup-flow`)
3. Create the folder: `public/data/projects/<project>/prototypes/<id>/`
4. Create `meta.json`:
   ```json
   { "name": "Signup Flow", "description": "...", "thumbnail": "preview.png" }
   ```
   (`thumbnail` is optional — image file in the prototype folder, used on the project hub.)
5. Add an entry to `public/data/projects/<project>/prototypes/index.json`:
   ```json
   {
     "id": "<id>",
     "name": "Signup Flow",
     "description": "...",
     "device": "mobile"
   }
   ```
6. Create `index.html` with design system imports:
   ```html
   <link rel="stylesheet" href="../../../../../styles/shared.css" />
   <link rel="stylesheet" href="../../../../../styles/ds.css" />
   ```
   **Path depth:** use exactly **five** `../` segments (`../../../../../`), not six. The prototype file lives at `data/projects/<project>/prototypes/<id>/index.html` under the site root; six levels escapes past the repo base on **GitHub project Pages** and breaks CSS (browser requests `/styles/...` on the `github.io` host instead of `/repo-name/styles/...`). Company CSS, if any: `../../../../../data/design-system/company.css`.

## Prototype Mode -- Interactive Features

Files in `public/data/projects/<project>/prototypes/<prototype>/` are interactive prototypes.

### How to detect prototype mode

The designer is in prototype mode when the file path matches `prototypes/*/`.

### What a prototype is

A prototype is a **self-contained interactive mini web app**. It lives in a single `index.html` (or a small set of files in the same folder). The designer prompts you to build it, and it handles its own:

- Form state (inputs, validation, error messages)
- Navigation between views/steps (tabs, multi-step flows)
- Animations and transitions
- Conditional rendering (show/hide based on state)
- Any interactive behavior that makes it feel like a real app feature

### Rules for prototype mode

1. **Read context first.** Read `meta.json` in the prototype folder to understand the prototype's name and purpose.
2. **JavaScript is expected.** Prototypes are interactive -- write real JS for state, transitions, validation, etc.
3. **Use hash routing for navigation.** Every prototype with multiple views/steps **must** use `location.hash` so each view gets a unique URL. This is required for usability testing tools (e.g. Maze) that track which screen a tester is on. See "Hash routing pattern" below.
4. **Use the design system.** Import styles using relative paths (5 levels up from the prototype folder). Use tokens and component classes as the foundation.
5. **Add new styles inline.** If the designer asks for something not in the design system, use `<style>` blocks inside the prototype. Keep the shared design system files untouched.
6. **Hide horizontal scrollbars** on carousels, chip rows, and other horizontal scrollers — use **`ds-scroll-x`** from `shared.css` (see canvas screen rules). Prefer **`overflow-x: clip`** on the root if the document gets a horizontal scrollbar.
7. **Self-contained.** Everything the prototype needs goes in its folder. No external npm packages or CDN scripts unless the designer explicitly asks.
8. **Look at the design system.** Read existing component HTML files in `public/data/design-system/components/` for visual patterns.
9. **One file preferred.** Keep the prototype in a single `index.html` unless it grows large enough to justify splitting.

### Hash routing pattern

All prototypes with multiple views must use hash-based routing so that each view has a distinct URL (e.g. `index.html#dashboard`, `index.html#transfer-form`, `index.html#success`). This enables usability testing tools like Maze to track screen transitions and measure task completion.

**How it works:**

1. Give each view container an `id` prefixed with `view-` (e.g. `id="view-dashboard"`, `id="view-transfer"`, `id="view-success"`).
2. Use a `navigate(viewId)` function instead of directly toggling classes or `hidden`. This function sets `location.hash`, which triggers the `hashchange` listener to show/hide views.
3. Add a `hashchange` listener and an initial route check on `DOMContentLoaded`.

**Standard boilerplate** -- include this in every multi-view prototype:

```html
<script>
  (function () {
    var defaultView = "dashboard";

    function navigate(viewId) {
      location.hash = viewId;
    }

    function applyRoute() {
      var hash = location.hash.slice(1) || defaultView;
      document.querySelectorAll(".view").forEach(function (v) {
        v.classList.toggle("active", v.id === "view-" + hash);
      });
    }

    window.addEventListener("hashchange", applyRoute);
    applyRoute();

    // Expose navigate globally for click handlers
    window.navigate = navigate;
  })();
</script>
```

**CSS for views:**

```css
.view {
  display: none;
}
.view.active {
  display: flex;
} /* or block, grid -- whatever fits the layout */
```

**Using it in click handlers:**

```html
<button onclick="navigate('transfer')">Make a transfer</button>
```

Or in JS:

```js
document.getElementById("btn-next").addEventListener("click", function () {
  navigate("review");
});
```

**Naming conventions for hash values:**

- Use lowercase, hyphenated names: `#dashboard`, `#transfer-form`, `#select-account`, `#review`, `#success`
- Keep them short and descriptive -- they appear in Maze analytics
- For sub-steps within a flow (e.g. picking a "from" account inside a transfer), use a compound name: `#transfer-from`, `#transfer-to`, `#transfer-review`

**Complex prototypes with multiple independent flows:**

Some prototypes have a main view (e.g. sidebar nav switching between Dashboard, Transfers, Payments) plus sub-flows (e.g. a multi-step transfer form). Use a flat hash namespace with descriptive prefixes:

- `#dashboard` -- main dashboard view
- `#transfers` -- transfers hub
- `#transfer-form` -- transfer form step
- `#transfer-from` -- pick "from" account
- `#transfer-to` -- pick "to" account
- `#transfer-review` -- review step
- `#transfer-success` -- confirmation
- `#payments` -- payments hub
- `#pay-bill` -- bill pay form

The `applyRoute()` function can be extended to handle hierarchical state (showing the right sidebar nav item, entering/exiting sub-flows) based on the hash prefix.

**Back button support:** Hash routing gives browser back/forward navigation for free. Users (and Maze testers) can use the back button to return to previous steps.

## Platform-Aware Components

When the designer specifies a target platform (iOS, Android, or desktop), adapt the UI accordingly:

1. **Check the design system first.** Look at `public/data/design-system/components/` and `company.css` for existing components that match the platform. If the team has built components, always prefer those.
2. **If no matching component exists**, build native-looking UI for the specified platform using the team's brand colors and tokens from `company.css` (or `shared.css` if no company overrides exist):
   - **iOS** -- SF-style navigation bars, large titles, rounded grouped list rows, iOS-style toggles, tab bars with icons, subtle separators, system-like blur effects. Use the team's accent color for tint/interactive elements.
   - **Android / Material** -- Top app bars, FABs, bottom navigation with labels, outlined/filled text fields, Material-style cards with subtle elevation, ripple-like interaction areas. Use the team's primary color for Material theming.
   - **Desktop** -- Sidebar navigation, multi-column layouts, data tables, breadcrumbs, dropdown menus, toolbar actions. Use wider content areas (1024–1440px frames).
3. **Always use the team's brand colors.** Native-looking doesn't mean default blue iOS or purple Material -- it means the platform's layout patterns and interaction idioms rendered in the team's color palette.
4. **If no platform is specified**, default to a generic/neutral style using the design system's existing components.

## Design Tokens (quick reference)

- Backgrounds: `var(--bg-0)` (#ffffff), `var(--bg-1)` (#f8f9fb)
- Accent: `var(--neon-blue)` (#3b82f6), `var(--neon-teal)` (#06b6d4)
- Text: `var(--text)`, `var(--muted)`, `var(--muted-2)` (dark text tokens)
- Border: `var(--border)`
- Radii: `var(--radius-sm)` 10px, `var(--radius-md)` 16px, `var(--radius-lg)` 22px, `var(--radius-xl)` 32px
- Font: Plus Jakarta Sans (loaded via Google Fonts in shared.css)

## Available Component Classes

The design system ships with generic/foundational components. Teams add their own domain-specific components on top of these.

### Buttons

`.btn`, `.btn-primary`, `.btn-dark`, `.btn-sm`

### Chips

`.ds-chip`, `.ds-chip.is-active`, `.ds-chip-row`

### Tags

`.ds-tag`, `.ds-tag-row`, `.ds-tag-tone-sunset`, `.ds-tag-tone-teal`, `.ds-tag-tone-green`, `.ds-tag-tone-mint`, `.ds-tag-tone-gold`, `.ds-tag-tone-dark`, `.ds-tag-tone-gray`, `.ds-tag-tone-navy`

### Badges & status

`.ds-badge`, `.ds-badge-red`, `.ds-badge-blue`, `.ds-badge-green`, `.ds-status-dot`, `.ds-status-dot-online`, `.ds-status-dot-away`, `.ds-status-dot-offline`

### Form inputs

`.form-group`, `.form-group label`, `.form-group input`, `.ds-search-bar`

### Toggle

`.ds-toggle`, `.ds-toggle.is-on`

### Navigation

`.ds-nav-bar`, `.ds-nav-bar-back`, `.ds-nav-bar-title`, `.ds-nav-bar-action`, `.ds-tab-bar`, `.ds-tab-item`, `.ds-tab-item.is-active`, `.ds-bottom-sheet`, `.ds-bottom-sheet-handle`

### Lists

`.ds-list`, `.ds-list-item`, `.ds-list-item-icon`, `.ds-list-item-content`, `.ds-list-item-label`, `.ds-list-item-desc`, `.ds-list-item-chevron`

### Avatars

`.ds-avatar-group`, `.ds-avatar-group-more`

### Section titles

`.ds-section-title`, `.ds-home-header`

### Feedback

`.ds-progress`, `.ds-progress-fill`, `.ds-skeleton`, `.ds-skeleton-text`, `.ds-skeleton-circle`, `.ds-skeleton-card`, `.ds-alert`, `.ds-alert-title`, `.ds-alert-body`, `.ds-alert-actions`, `.ds-snackbar`, `.ds-snackbar-action`, `.ds-empty-state`, `.ds-empty-state-icon`, `.ds-empty-state-title`, `.ds-empty-state-desc`

### Layout

`.ds-divider`, `.ds-divider-text`

`.ds-scroll-x` — horizontal scrolling container with scrollbars hidden (chip rows, carousels); use on canvas screens and prototypes per canvas rules.

## How to Create Design System Components

The design system starts empty. When a designer asks for components -- either by describing them, sharing screenshots, or asking for specific UI patterns -- follow these steps.

### Creating a component

**In the core repo** (developing the tool):

1. **Add CSS classes** to `public/styles/ds.css`. Use design tokens from `shared.css` (colors, radii, fonts, spacing). Follow the naming convention: `.ds-<component>` for the main class, `.ds-<component>-<part>` for sub-elements.
2. **Create a preview HTML file** at `public/data/design-system/components/<name>.html`. This is a pure HTML fragment -- no `<!doctype>`, no `<html>`, no `<head>`, no `<body>`, no style imports. The design system page injects it into a container that already has `shared.css` and `ds.css` loaded.
3. **Register it** in `public/data/design-system/registry.json` -- add it to an existing group/category or create new ones.

**In a company repo** (using the tool):

1. **Create a preview HTML file** at `public/data/design-system/components/<company-slug>/<name>.html`. Same format -- pure HTML fragment. Use inline styles or classes from `company.css` for company-specific styling.
2. **Register it** in `public/data/design-system/registry.json` with `"file": "<company-slug>/<name>.html"`.
3. **If you need new CSS classes**, add them to `public/data/design-system/company.css` (not `ds.css`). Prefix with the company slug (e.g. `.jh-card`). Or use inline styles in the preview HTML -- that's fine for simple components.

### Preview HTML file format

- Pure HTML fragment (no document structure tags)
- Can use inline styles for layout within the preview (e.g. wrapping in a flex container)
- Must use CSS classes from `ds.css` and tokens from `shared.css`
- Keep it minimal -- just enough to show the component in context
- Example for a button preview:
  ```html
  <div style="display: flex; flex-wrap: wrap; gap: 12px;">
    <button class="btn btn-primary">Primary</button>
    <button class="btn btn-primary btn-sm">Primary Small</button>
  </div>
  ```

### Registry structure

The `registry.json` file controls the design system page layout:

```json
{
  "groups": [
    { "id": "foundations", "name": "Foundations" },
    { "id": "inputs", "name": "Inputs & Controls" }
  ],
  "categories": [
    {
      "group": "foundations",
      "name": "Colors",
      "layout": "row",
      "components": [
        { "id": "colors-bg", "file": "colors-bg.html", "label": "Backgrounds" }
      ]
    }
  ]
}
```

- **Groups** are top-level sections that stack vertically. Each gets a header row.
- **Categories** belong to a group and appear as horizontal cards under that group.
- Each category has a `components` array -- each component gets its own preview file.
- Use `"layout": "row"` on a category to lay out its components side-by-side (good for color swatches, button variants, icon sets).
- Group IDs should be lowercase, hyphenated (e.g. `cards-content`, `inputs-controls`).

### User content separation

All user-created content in a company repo must stay cleanly separated from tool files. The rule is simple: **everything the team creates goes under `public/data/`**. Never edit tool-level files (`shared.css`, `ds.css`, `public/scripts/`, root HTML pages, etc.).

#### Design system components: use a company subfolder

Put components in `public/data/design-system/components/<company-slug>/`, not directly in `components/`. The company slug comes from the repo name (e.g. `design-core-acme` → `acme`).

- **Path**: `public/data/design-system/components/<company-slug>/<component>.html` (e.g. `components/acme/card-payment.html`)
- **Registry**: Set `file` to include the subfolder: `"file": "acme/card-payment.html"`

This avoids merge conflicts when pulling upstream and makes it obvious which components belong to the team.

#### Brand overrides: use `company.css`

**Never edit `shared.css` or `ds.css` in a company repo.** Those are tool files that update via upstream pulls.

Instead, create `public/data/design-system/company.css` for brand-specific overrides. The design system page (and prototypes) auto-load this file if it exists. It loads after `shared.css` and `ds.css`, so overrides take effect via CSS specificity.

Use it for:

- **Colors**: Override `--bg-0`, `--bg-1`, `--neon-blue`, `--neon-blue-rgb`, `--neon-teal`, `--neon-teal-rgb`, `--text`, `--muted`, `--muted-2`, or add new brand tokens
- **Typography**: Override `--font-body`, `--font-display`, add `@import` for Google Fonts
- **Shape**: Override `--radius-sm`, `--radius-md`, `--radius-lg`, `--radius-xl`, `--shadow`, `--shadow-soft`
- **New component classes**: Add company-specific CSS classes (prefix with your company slug, e.g. `.jh-card`)

Example `company.css`:

```css
@import url("https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600;700&display=swap");

:root {
  --neon-blue: #2563eb;
  --neon-blue-rgb: 37, 99, 235;
  --font-body: "Open Sans", sans-serif;
}
```

### When the designer shares screenshots or describes their brand

This is a common flow: the designer shows what their app looks like and wants the design system to match. Follow this process:

1. **Extract colors** from the screenshots or description. Map them to CSS custom properties in `public/data/design-system/company.css` (NOT `shared.css`):
   - Override `--bg-0`, `--bg-1` for backgrounds
   - Override `--neon-blue`, `--neon-blue-rgb` for the primary accent
   - Override `--neon-teal`, `--neon-teal-rgb` for the secondary accent
   - Add new tokens for any additional brand colors (e.g. `--brand-red`, `--brand-red-rgb`)
   - Override `--text`, `--muted`, `--muted-2` for text hierarchy
2. **Extract typography** -- if they want different fonts, add the `@import` and override `--font-body` and `--font-display` in `company.css`.
3. **Extract spacing and shape** -- override `--radius-*` tokens if their design uses different corner radii. Override `--shadow` and `--shadow-soft` if they use different elevation styles.
4. **Build matching components** using the updated tokens. Each component needs:
   - Preview HTML file in `components/<company-slug>/`
   - Entry in `registry.json`
   - Optionally, CSS classes in `company.css` (prefix with company slug)

### Color component conventions

When creating color palette components for the design system page, render visual swatches so they look good on the design system page.

**Layout — use a grid, not a vertical stack.**

- **Do not** stack swatches in a single column (e.g. one swatch per row). Use a **grid layout** so multiple swatches sit side-by-side and wrap into rows.
- **Preferred:** CSS Grid with a fixed or responsive column count so swatches align in a neat grid:
  - Example: `display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px;` for a 4-column grid (use 4–6 columns depending on swatch size).
  - Or responsive: `grid-template-columns: repeat(auto-fill, minmax(72px, 1fr)); gap: 12px;` so columns adapt to container width.
- **Alternative:** Flexbox with wrap is acceptable: `display: flex; flex-wrap: wrap; gap: 12px;` — but ensure the container is wide enough that multiple swatches fit per row (do not use a narrow column or `flex-direction: column` for the swatch list).

**Swatch structure (per color):**

- Square (or rounded square) showing the color — e.g. `width` and `height` 48–56px, `border-radius: 10px`, optional thin border for light colors (`border: 1px solid rgba(0,0,0,0.1)`).
- Label below: token name and hex (or value), small font (e.g. `font-size: 0.65rem`), muted color.

**Example (grid of swatches):**

```html
<div style="display:grid;grid-template-columns:repeat(4, 1fr);gap:12px;">
  <div style="display:flex;flex-direction:column;align-items:center;gap:4px;">
    <div
      style="width:56px;height:56px;border-radius:10px;background:var(--bg-0);border:1px solid rgba(0,0,0,0.1);"
    ></div>
    <span style="font-size:0.65rem;font-weight:700;color:rgba(0,0,0,0.45);"
      ><strong>BG 0</strong> #ffffff</span
    >
  </div>
  <!-- repeat for each color -->
</div>
```

Group colors logically: backgrounds, accent/brand colors, text colors, semantic colors (success, error, warning).

### Suggested default groups

When building a design system from scratch, use these groups as a starting point (add or rename as needed):

- **Foundations** -- colors, typography, spacing, shadows
- **Inputs & Controls** -- buttons, form fields, toggles, chips, search
- **Feedback & Status** -- badges, tags, alerts, progress, skeleton loaders, toasts
- **Navigation** -- nav bars, tab bars, list items, bottom sheets
- **Cards & Content** -- any domain-specific card types, content rows, media layouts
- **Layout & Structure** -- dividers, avatar groups, section headers

## Screen Captures

The Captures page lets teams screenshot their live app and view the results on an infinite canvas. Configuration is split into two files:

### Shared config (committed to git)

**`public/data/captures/config.json`** -- contains `appUrl`, `viewport`, `discover`, `screens`, `dismissSelectors`. Created automatically when a designer captures from the UI, or you can create/edit it directly. This is shared with the whole team.

### Local credentials (gitignored)

**`.app-screens.json`** in the repo root -- only needed for apps behind login. Contains the `login` block with steps and credentials. Each designer creates their own. Copy from `.app-screens.example.json`.

### How to help designers with captures

1. **First capture (public app)** -- tell them to go to the Captures page and enter their URL. Or create `public/data/captures/config.json` with `appUrl` and `"discover": true`.

2. **App behind login** -- create `.app-screens.json` in the repo root. Ask the designer for their login credentials. The simple format auto-detects login fields:

   ```json
   { "login": { "username": "their_username", "password": "their_password" } }
   ```

   Add `"url": "/custom-login"` if the login page isn't at `/login`. The script auto-detects username/email fields, password fields, submit buttons, and handles both single-page and multi-step (SSO) login flows. If MFA is required, the browser stays open for manual completion.

   If auto-detection doesn't work (custom web components, non-standard forms), fall back to explicit steps:

   ```json
   {
     "login": {
       "url": "/login",
       "steps": [
         {
           "action": "fill",
           "selector": "input[name='username']",
           "value": "user@example.com"
         },
         { "action": "submit" },
         {
           "action": "fill",
           "selector": "input[type='password']",
           "value": "password123"
         },
         { "action": "submit" }
       ]
     }
   }
   ```

3. **Custom dismiss selectors** -- if the app has popups/modals that aren't auto-dismissed, add CSS selectors to `dismissSelectors` in the shared config.

4. **Explicit screens** -- if discovery mode misses screens or the designer wants specific ones, set `"discover": false` and list screens in the `screens` array with `name`, `path`, and optional `group`, `waitFor`, `delay`.

5. **Troubleshooting** -- if captures fail:
   - Check that Playwright is installed: `npx playwright install chromium`
   - Check that the URL is correct and accessible
   - For login issues, the browser window stays open so the designer can complete MFA manually
   - Check the terminal output for specific errors

### What you can edit (consumer repo)

- `public/data/captures/config.json` -- shared capture settings (this is user content under `public/data/`)
- `.app-screens.json` -- local credentials (gitignored)

Do NOT edit `scripts/capture-screens.js`, `captures.html`, or the Vite config -- those are tool files.

## Tone

> In **core repo mode**, speak technically -- the user is a developer. Skip the simplifications below.

> In **consumer repo mode**, follow these rules:

- Speak simply. Avoid technical terms like "DOM", "component tree", "state management".
- Say "screen" not "page" or "route".
- Say "design system" not "CSS framework".
- If a designer asks for something that requires tool code changes, tell them to reach out to **Brandon Unglaub** -- he maintains the core tool.
- Be proactive: suggest design system components that fit what they're building.
